language: python

python:
  - "2.7"

branches: 
  only:
    - develop
    - master
install:
    pip install -r requirements.txt

install:
    pip install -r dev-requirements.txt



services:
    - mongodb
    - sleep 15

script: py.test